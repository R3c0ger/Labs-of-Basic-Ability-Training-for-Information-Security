# Crypto 课程笔记

注意：
- 以下内容为本次课程实验中将会涉及的内容。
- 本次实验特别水，水到报告一个字都写不出来。
- 加星号的不是实验内容。

[TOC]

## 编码

### 莫斯编码

`.` 短 `-` 长 `/` 分隔（有些工具里用空格来表示分隔）

26 字母、数字、标点符号加密列表

`%u7b` 表示 `{`，`%u7d` 表示 `}`，`%ud` 表示 `_`，`%5c` 表示 `\` -->url code

### Base 64

基于 64 个可打印字符来表示二进制数据的方法。

每 3 个 8 bit 字节转换为 4 个 6 bit 的字节，不足 24 位后面补 0，6 bit 高位添两个零。

字符表：`ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/`（大写字母 A ~ Z，小写字母 a ~ z，数字 0 ~ 9 以及"+","/"）

- base 32：5 个 8 bit 转换为 8 个 5 bit，不满 40 bit 则补 0，5 bit 的 0 相当于 `=`
  字符表： `ABCDEFGHIJKLMNOPQRSTUVWXYZ234567

- base 16：4 个 8 bit 转换为 8 个 4 bit，不满 32 bit 则补 0，4 bit 的 0 相当于 `=`
  字符表：`0123456789ABCDEF`

### uuencode

将输入以 3 字节为单位编码，不足 3 字节则补零。

这三个字节共有 24 个 Bit，以 6bit 为单位分为 4 个群组，每个群组以十进制来表示所出现的数值只会落在 0 到 63 之间。将每个数加上 32，所产生的结果刚好落在 ASCII 字符集中可打印字符（32-空白...95-底线）的范围之中。 

Uuencode 编码每 60 个将输出为独立的一行（相当于 45 个输入字节），每行的开头会加上长度字符，除了最后一行之外，长度字符都应该是“M”这个 ASCII 字符（77=32+45），最后一行的长度字符为 32 + 剩下的字节数目这个 ASCII 字符。

## 古典密码

### 移位密码

栅栏密码

### 替代密码

凯撒

ROT 13

猪圈

维吉尼亚*

### 其他

键盘密码

跳舞小人*

